node_modules/
core/packages/
_packages/
*.transport.zip
.env
.DS_Store

# Vendor component sources for study only — never packaged or committed
_reference/

# Local-only AI working notes (dev brief + roadmap; internal, not part of the package)
AI_HANDOFF.md
AI_ROADMAP.md

# Local scratch notes — HOLDS STAND CREDENTIALS, must never reach this public repo
todo.txt

# Reference copy of a clean MODX install, for local study only — never committed
modx-example-2-8-8/
