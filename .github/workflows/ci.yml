name: CI

on:
  push:
    branches: [ main, modx3 ]
  pull_request:
  workflow_dispatch:

jobs:
  lint:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: PHP syntax check (php -l) on every PHP file
        run: |
          php -v
          # _reference/ and modx-example are gitignored (not in the repo); node_modules excluded defensively.
          find . -name '*.php' -not -path './node_modules/*' -print0 \
            | xargs -0 -n1 php -l

      - uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Client syntax check
        run: node --check client/index.js

      - name: Portability and safe-defaults check
        run: node _build/test.portability.js

      - name: Client/server contract check
        run: node _build/test.contract.js

      - name: Version consistency (build.config.php == package.json == package-lock.json)
        run: |
          PKG=$(php -r "include '_build/build.config.php'; echo PKG_VERSION;")
          NPM=$(node -p "require('./package.json').version")
          LOCK=$(node -p "require('./package-lock.json').version")
          MODEL=$(php -r "require 'core/components/modxmcp/model/modxmcp.class.php'; echo modxMCP::VERSION;")
          echo "build.config.php=$PKG  package.json=$NPM  package-lock.json=$LOCK  model::VERSION=$MODEL"
          if [ "$PKG" != "$NPM" ] || [ "$NPM" != "$LOCK" ] || [ "$LOCK" != "$MODEL" ]; then
            echo "::error::Version mismatch across build.config.php / package.json / package-lock.json / modxMCP::VERSION"
            exit 1
          fi

      - name: CHANGELOG has an entry for the current version
        run: |
          PKG=$(php -r "include '_build/build.config.php'; echo PKG_VERSION;")
          if ! grep -q "## ${PKG} " CHANGELOG.md; then
            echo "::error::CHANGELOG.md has no '## ${PKG}' entry"
            exit 1
          fi
          echo "CHANGELOG has an entry for ${PKG}."


  modx3-processors:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        modx_ref: [v3.0.0-pl, v3.2.4-pl, 3.x]
    steps:
      - uses: actions/checkout@v4

      - name: Check MODX 3 processor compatibility
        run: |
          git clone --depth 1 --branch "${{ matrix.modx_ref }}" https://github.com/modxcms/revolution.git "$RUNNER_TEMP/modx"
          php _build/test.modx3-processors.php "$RUNNER_TEMP/modx/core/src/Revolution/Processors"